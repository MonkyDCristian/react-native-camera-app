/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';

// Registrar el componente con el nombre correcto para Expo
AppRegistry.registerComponent('main', () => App);
AppRegistry.registerComponent('CameraAppNew', () => App);
